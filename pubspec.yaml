name: cuevana7_movies_app_cv
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  postgres: 2.6.1
  bcrypt: 1.2.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  go_router: 17.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/

  fonts:
    - family: InclusiveSans
      fonts:
        - asset: assets/fonts/InclusiveSans-VariableFont_wght.ttf
